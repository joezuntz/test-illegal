name: Continuous Integration

on:
  push:
    branches: [ main ]

jobs:
  Test:

    runs-on: ubuntu-latest

    container:
      image: joezuntz/txpipe-auto

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Import firecrown
      run: |
        ulimit -c unlimited
        sudo python -c "import faulthandler; faulthandler.enable(); import camb"
        sudo python -c "import faulthandler; faulthandler.enable(); import cosmosis"
        sudo python -c "import faulthandler; faulthandler.enable(); import firecrown"


    - name: ls
      if: ${{ always() }}
      run: |
        ls -R
        date
        ls -ltr /cores
